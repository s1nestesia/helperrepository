<h1>Followings</h1>

 
<ul>
	<% @followings.each do |following| %>
		<li>
			<%= link_to(following.email, user_path(following), class:"follow-user") %>
		</li>
	<% end %>
</ul>
<%= link_to("Back", user_path(@user), class:"btn btn-dark") %>
